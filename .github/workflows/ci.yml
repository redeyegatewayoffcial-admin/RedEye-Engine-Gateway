# name: CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   rust-checks:
#     name: Rust (Backend) Quality Gate
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Install Rust toolchain
#         uses: dtolnay/rust-toolchain@stable
#         with:
#           components: rustfmt, clippy

#       - name: Rust Cache
#         uses: Swatinem/rust-cache@v2
#         with:
#           prefix-key: "v1-rust"

#       - name: Enforce Formatting
#         run: cargo fmt --all -- --check

#       - name: Clippy Linting
#         run: cargo clippy --workspace -- -D warnings

#       - name: Run Tests
#         # Optimization: Limit threads to prevent resource contention in CI
#         run: cargo test --workspace --verbose -- --test-threads=2
#         env:
#           DOCKER_HOST: unix:///var/run/docker.sock

#   frontend-checks:
#     name: Frontend (Dashboard) Quality Gate
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: ./redeye_dashboard
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: './redeye_dashboard/package-lock.json'

#       - name: Install dependencies
#         run: npm ci

#       - name: Run Lint
#         run: npm run lint

#       - name: Run Vitest
#         run: npm test
